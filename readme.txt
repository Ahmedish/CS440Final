RUN INSTRUCTIONS

To run this program make sure you have a stable version of python 2 downloaded.

Type the following command into your terminal:

python dataClassifier.py -f -d faces -c nb


"faces" can be replaced with "digits"

"nb" can be replaced with either "mira" or "perceptron"


